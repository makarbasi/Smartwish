# env.example - example environment variables for SmartWish
# Based on smartwish-frontend/.env.local. Do NOT commit real secrets.
# Copy this file to the appropriate .env file for each subproject and fill in real values.

# Gemini (Generative Language) API key (server-side only)
GEMINI_API_KEY=YOUR_GEMINI_API_KEY

# Tremendous API key for gift card marketplace (server-side only)
# Get this from your Tremendous account: https://tremendous.com/
TREMENDOUS_API_KEY=YOUR_TREMENDOUS_API_KEY

# Public base URL used by the frontend component. Defaults to production SmartWish.
# Set to http://localhost:3000 when developing locally and calling the local Next API route.
NEXT_PUBLIC_API_BASE=https://smartwish.onrender.com

# Optional: change runtime behavior
# NODE_ENV=development

# Notes:
# - Replace placeholders with real values for your environment.
# - Keep secrets (GEMINI_API_KEY, TREMENDOUS_API_KEY) out of source control.
# - For Tremendous API, use the test environment API key for development

