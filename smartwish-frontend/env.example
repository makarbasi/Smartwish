# env.example - example environment variables for SmartWish
# Based on smartwish-frontend/.env.local. Do NOT commit real secrets.
# Copy this file to the appropriate .env file for each subproject and fill in real values.

# Gemini (Generative Language) API key (server-side only)
GEMINI_API_KEY=YOUR_GEMINI_API_KEY

# Tremendous API key for gift card marketplace (server-side only)
# Get this from your Tremendous account: https://tremendous.com/
TREMENDOUS_API_KEY=YOUR_TREMENDOUS_API_KEY

# Tremendous API Base URL - switch between sandbox and production
# Production: https://www.tremendous.com/api/v2
# Sandbox: https://testflight.tremendous.com/api/v2
TREMENDOUS_BASE_URL=https://www.tremendous.com/api/v2

# Tillo API Configuration (server-side only)
# Get these from your Tillo account: https://hub.sandbox.tillo.dev/ (sandbox) or https://hub.tillo.io/ (production)
# API Key - Your unique API identifier
TILLO_API_KEY=YOUR_TILLO_API_KEY
# API Secret - Used for HMAC signature generation (only shown once when created!)
TILLO_API_SECRET=YOUR_TILLO_API_SECRET
# Base URL - switch between sandbox and production
# Sandbox: https://sandbox.tillo.dev/api/v2
# Production: https://api.tillo.io/api/v2
TILLO_BASE_URL=https://sandbox.tillo.dev/api/v2

# Public base URL used by the frontend component. Defaults to production SmartWish.
# Set to http://localhost:3000 when developing locally and calling the local Next API route.
NEXT_PUBLIC_API_BASE=https://smartwish.onrender.com

# NextAuth Configuration (REQUIRED for authentication)
# Generate AUTH_SECRET by running: openssl rand -base64 32
# Or use: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
AUTH_SECRET=your-generated-auth-secret-here

# Google OAuth (optional, for Google sign-in)
# Get these from https://console.cloud.google.com/apis/credentials
AUTH_GOOGLE_ID=your-google-client-id
AUTH_GOOGLE_SECRET=your-google-client-secret

# ============================================
# Gift Card Data Encryption (REQUIRED for production)
# ============================================
# AES-256 encryption key for gift card data at rest
# Generate with: openssl rand -hex 32
# CRITICAL: In production, use AWS KMS, HashiCorp Vault, or Azure Key Vault
#           DO NOT store this in .env files in production!
ENCRYPTION_KEY=your-64-character-hex-key-here-generate-with-openssl-rand-hex-32

# Key rotation support - when rotating keys:
# 1. Generate new key: openssl rand -hex 32
# 2. Set as ENCRYPTION_KEY_V2 (increment version)
# 3. Keep old key as ENCRYPTION_KEY_V1 for decrypting old data
# 4. After all data is re-encrypted, remove old key
# ENCRYPTION_KEY_V1=old-key-after-rotation
# ENCRYPTION_KEY_V2=new-key-after-rotation

# Optional: change runtime behavior
# NODE_ENV=development

# Notes:
# - Replace placeholders with real values for your environment.
# - Keep secrets (GEMINI_API_KEY, TREMENDOUS_API_KEY, AUTH_SECRET, AUTH_GOOGLE_SECRET) out of source control.
# - For Tremendous API, use the test environment API key for development
# - AUTH_SECRET is REQUIRED - authentication will fail with "Configuration" error if missing

