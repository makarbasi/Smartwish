# 🎨 System Overview - Visual Guide

## 📊 Data Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    Your Card Folders                             │
│  C:\...\Designs\Series1_original\Series\                        │
│                                                                  │
│  ┌──────────────────┐  ┌──────────────────┐                    │
│  │ BirthdayCardBasic│  │  BirthdayFloral  │  ... more folders  │
│  ├──────────────────┤  ├──────────────────┤                    │
│  │ metadata.json    │  │ metadata.json    │                    │
│  │ insidenote.json  │  │ insidenote.json  │                    │
│  │ card_1.png       │  │ card_1.png       │                    │
│  │ inside_card_1.png│  │ inside_card_1.png│                    │
│  └──────────────────┘  └──────────────────┘                    │
└─────────────────────────────────────────────────────────────────┘
                           ↓
                           ↓ Read JSON files
                           ↓
┌─────────────────────────────────────────────────────────────────┐
│              update_sw_templates.py Script                       │
│                                                                  │
│  1. Scan all folders                                            │
│  2. Read metadata.json + insidenote.json                        │
│  3. Extract card data                                           │
│  4. Map to categories                                           │
│  5. Construct storage URLs                                      │
│  6. Build complete records                                      │
│  7. Check for duplicates                                        │
│  8. Batch UPSERT to database                                    │
└─────────────────────────────────────────────────────────────────┘
                           ↓
                           ↓ Upload via Supabase API
                           ↓
┌─────────────────────────────────────────────────────────────────┐
│                    Supabase Database                             │
│                                                                  │
│  ┌──────────────┐   ┌──────────────┐   ┌──────────────┐       │
│  │sw_categories │   │ sw_authors   │   │ sw_templates │       │
│  ├──────────────┤   ├──────────────┤   ├──────────────┤       │
│  │ Birthday     │   │ Smartwish    │   │ 221 cards    │       │
│  │ Thankyou     │   │ Studio       │   │ with full    │       │
│  │ Graduation   │   │              │   │ metadata     │       │
│  │ ...          │   │              │   │ + embeddings │       │
│  └──────────────┘   └──────────────┘   └──────────────┘       │
│         ↑                  ↑                                    │
│         └──────────────────┴─────── Foreign Keys               │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Processing Pipeline

```
📁 Card Folder
    ↓
    ├─→ metadata.json ─────────┐
    │   • title                 │
    │   • description           │
    │   • occasion              │
    │   • recipient             │
    │   • style                 │
    │   • keywords              │
    │                          │
    ├─→ insidenote.json ───────┤
    │   • inside_note           │
    │   • embedding (768D)      │
    │                          │
    └─→ Image Files ───────────┤
        • card_1.png            │
        • inside_card_1.png     │
                                ↓
                    ┌─────────────────────┐
                    │  Script Processing  │
                    ├─────────────────────┤
                    │ 1. Extract data     │
                    │ 2. Map category     │
                    │ 3. Build URLs       │
                    │ 4. Create slug      │
                    │ 5. Validate         │
                    └─────────────────────┘
                                ↓
                    ┌─────────────────────┐
                    │   Database Record   │
                    ├─────────────────────┤
                    │ title               │
                    │ slug                │
                    │ category_id         │
                    │ author_id           │
                    │ description         │
                    │ price: 2.99         │
                    │ cover_image (URL)   │
                    │ image_1 (URL)       │
                    │ image_2 (URL)       │
                    │ image_3 (blank)     │
                    │ image_4 (logo)      │
                    │ message             │
                    │ keywords[]          │
                    │ embedding_vector[]  │
                    │ target_audience     │
                    │ occasion_type       │
                    │ style_type          │
                    │ status: published   │
                    └─────────────────────┘
                                ↓
                        Supabase sw_templates
```

---

## 🗂️ Category Mapping

```
Local Folder Name          →  Category  →  Storage URL
─────────────────────────────────────────────────────────────
BirthdayCardBasic          →  Birthday  →  .../Birthday/BirthdayCardBasic/
BirthdayFloral             →  Birthday  →  .../Birthday/BirthdayFloral/
BirthdayFunny              →  Birthday  →  .../Birthday/BirthdayFunny/
Congratulations            →  Congrats  →  .../Congratulation/
ChristmasCardBundle        →  Holidays  →  .../Holidays/
Thanksgiving               →  Thanks.   →  .../Thanksgiving/
Thankyou                   →  Thankyou  →  .../Thankyou/
Graduation                 →  Graduatn  →  .../Graduation/
```

---

## 🎯 Script Execution Flow

```
User runs script
    ↓
┌────────────────────────────┐
│ 1. Initialize Supabase     │
│    Connection              │
└────────────────────────────┘
    ↓
┌────────────────────────────┐
│ 2. Scan Card Directory     │
│    Find all subfolders     │
└────────────────────────────┘
    ↓
┌────────────────────────────┐
│ 3. For Each Folder:        │
│    ├─ Load metadata.json   │
│    ├─ Load insidenote.json │
│    ├─ Match by filename    │
│    └─ Extract card data    │
└────────────────────────────┘
    ↓
┌────────────────────────────┐
│ 4. Get Category ID         │
│    from sw_categories      │
└────────────────────────────┘
    ↓
┌────────────────────────────┐
│ 5. Get Author ID           │
│    from sw_authors         │
└────────────────────────────┘
    ↓
┌────────────────────────────┐
│ 6. For Each Card:          │
│    ├─ Generate slug        │
│    ├─ Check duplicate      │
│    ├─ Build record         │
│    ├─ Construct URLs       │
│    └─ Add to batch         │
└────────────────────────────┘
    ↓
┌────────────────────────────┐
│ 7. Batch UPSERT            │
│    to sw_templates         │
└────────────────────────────┘
    ↓
┌────────────────────────────┐
│ 8. Log Results             │
│    ├─ Successful uploads   │
│    ├─ Skipped duplicates   │
│    └─ Failed cards         │
└────────────────────────────┘
    ↓
Complete!
```

---

## 🔍 Duplicate Detection

```
New Card
    ↓
Generate slug from filename
    ↓
Check sw_templates for existing slug
    ↓
    ├─ EXISTS ─────→ Skip (log as skipped)
    │
    └─ NOT EXISTS ─→ Proceed with upload
```

---

## 📦 Record Structure

```
sw_templates Record
├── Identifiers
│   ├── id (UUID, auto-generated)
│   ├── slug (unique, from filename)
│   └── title (from metadata or generated)
│
├── Relationships
│   ├── category_id (FK → sw_categories)
│   └── author_id (FK → sw_authors)
│
├── Content
│   ├── description (from metadata)
│   ├── message (from insidenote)
│   └── search_keywords[] (from metadata)
│
├── Images
│   ├── cover_image (Supabase URL)
│   ├── image_1 (main card)
│   ├── image_2 (inside card)
│   ├── image_3 (blank page)
│   └── image_4 (logo page)
│
├── Classification
│   ├── occasion_type (from metadata)
│   ├── style_type (from metadata)
│   └── target_audience (from metadata)
│
├── AI/Search
│   ├── embedding_vector[768] (from insidenote)
│   └── embedding_updated_at (timestamp)
│
├── Metadata
│   ├── price (default 2.99)
│   ├── is_featured (default false)
│   └── status (default 'published')
│
└── Timestamps
    ├── created_at (auto)
    └── updated_at (auto)
```

---

## 🚀 Usage Workflow

```
FIRST TIME
┌─────────────────────┐
│ 1. Setup Database   │ → Run setup_database.sql in Supabase
└─────────────────────┘
         ↓
┌─────────────────────┐
│ 2. Verify Setup     │ → python verify_setup.py
└─────────────────────┘
         ↓
┌─────────────────────┐
│ 3. Dry Run          │ → python update_sw_templates.py --dry-run
└─────────────────────┘
         ↓
┌─────────────────────┐
│ 4. Upload           │ → python update_sw_templates.py
└─────────────────────┘
         ↓
┌─────────────────────┐
│ 5. Verify Results   │ → Check Supabase dashboard
└─────────────────────┘

SUBSEQUENT UPDATES
┌─────────────────────┐
│ 1. Add New Cards    │ → Add to folders with JSON
└─────────────────────┘
         ↓
┌─────────────────────┐
│ 2. Generate Meta    │ → (if needed)
└─────────────────────┘
         ↓
┌─────────────────────┐
│ 3. Upload           │ → python update_sw_templates.py
└─────────────────────┘
```

---

## 🛡️ Error Handling

```
Error Occurs
    ↓
    ├─ JSON Load Error ────→ Log warning, skip folder, continue
    │
    ├─ Missing Category ───→ Log error, skip folder, continue
    │
    ├─ Missing Author ─────→ Log error, skip folder, continue
    │
    ├─ Database Error ─────→ Log error, skip card, continue
    │
    └─ Unexpected Error ───→ Log error, provide details, exit
```

---

## 📊 System Components

```
┌─────────────────────────────────────────────────────────┐
│                   DOCUMENTATION LAYER                    │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │ Quick Start  │  │   README     │  │  Checklist   │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└─────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────┐
│                   AUTOMATION LAYER                       │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │ PowerShell   │  │   Batch      │  │   Python     │  │
│  │   Script     │  │   Script     │  │   Scripts    │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└─────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────┐
│                     CORE LAYER                           │
│  ┌──────────────┐  ┌──────────────┐                     │
│  │   Update     │  │   Verify     │                     │
│  │   Script     │  │   Script     │                     │
│  └──────────────┘  └──────────────┘                     │
└─────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────┐
│                    DATA LAYER                            │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │ Local JSON   │  │  Supabase    │  │   Storage    │  │
│  │   Files      │  │  Database    │  │   URLs       │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└─────────────────────────────────────────────────────────┘
```

---

## 🎨 Visual Summary

```
   📁 Local Cards       🔄 Script          🗄️ Supabase
   ─────────────       ──────────        ──────────────
   221 cards      →    Read JSON    →    sw_templates
   8 folders      →    Map data     →    With metadata
   JSON files     →    Upload       →    + Embeddings
                                         + URLs
```

---

## 💡 Key Features

```
✅ AUTOMATION
   └─ Batch processing of all folders
   └─ Automatic slug generation  
   └─ Storage URL construction
   
✅ DATA QUALITY
   └─ JSON validation
   └─ Duplicate detection
   └─ Required field checks
   
✅ ERROR HANDLING
   └─ Graceful failure recovery
   └─ Detailed error logging
   └─ Folder-level isolation
   
✅ USER EXPERIENCE
   └─ Dry run mode
   └─ Progress indicators
   └─ Comprehensive logging
   └─ Interactive scripts
```

---

**This visual guide helps you understand how the system works at a glance!**

For step-by-step instructions, see [QUICK_START_UPDATE.md](QUICK_START_UPDATE.md)

